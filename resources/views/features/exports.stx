<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('features', 'exports')!
const canonical = 'https://reportshq.org/features/exports'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Features', href: '/features' },
  { label: page.title },
]

const sections = [
  {
    "heading": "A shape a spreadsheet can use",
    "body": "Exports come out in long format: one row per series per period, rather than a grid that looks right on screen and fights every pivot table. A wide export is easier to read and harder to use, and an export exists to be used."
  },
  {
    "heading": "CSV and XLSX",
    "body": "CSV for anything that reads text, XLSX when somebody wants a workbook with the types already correct so the dates are dates. Both come from the same query the chart ran, so an exported number and a drawn number cannot disagree."
  },
  {
    "heading": "Download links that expire",
    "body": "A generated file is reachable through a signed link with a deadline on it, and the file is deleted when the link dies. Keeping somebody’s numbers on disk past the life of the only URL that reaches them is storage nobody asked for."
  },
  {
    "heading": "Scheduled delivery, in your timezone",
    "body": "The headline numbers by email, daily, weekly or monthly, at an hour you choose in your project’s own timezone. The hour stays the hour after the clocks change, because the schedule is evaluated in that timezone rather than by adding an offset to UTC and hoping."
  },
  {
    "heading": "Recipients are checked",
    "body": "A schedule can only send to addresses tied to the project. A reporting tool that will email anything anywhere on a timer is a data exfiltration feature with a friendly name."
  }
]
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">Put the numbers where the work happens</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          CSV is on every plan. Scheduled delivery and XLSX come with the paid tiers, and the pricing page states exactly which.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
