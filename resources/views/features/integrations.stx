<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('features', 'integrations')!
const canonical = 'https://reportshq.org/features/integrations'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Features', href: '/features' },
  { label: page.title },
]

const sections = [
  {
    "heading": "One package, inside the application",
    "body": "composer require, a migration, and a config file naming your models. There is nothing to deploy beside it and no second service to keep upgrading in step with the app it reports on."
  },
  {
    "heading": "It reads what you already have",
    "body": "No tracking calls, no event names to agree on, no schema to keep in sync. Point it at an Order, a Customer, a Subscription. Those already exist, they are already correct, and they are already the thing you would have been describing anyway."
  },
  {
    "heading": "Nothing leaves your servers",
    "body": "Queries run in process, on the database connection the application already holds. There is no connection to hand out, no credential to rotate, and no endpoint to send anything to. The licence check is offline too."
  },
  {
    "heading": "Where it renders",
    "body": "Standalone pages behind whatever middleware you already use, or inside a Filament panel, or as JSON for a front end of your own. The charts are the same components in all three, so they cannot drift."
  }
]
const shot = '/img/feature-integrations.png'
const shotAlt = 'A terminal showing three install commands, and a config file naming an Order model with its measures, time keys and dimensions'
const shotCaption = 'The whole installation, and the config the compiler reads. Copied from a working application rather than written for the page.'
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      @include('FeatureShot')

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">Start with one endpoint</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          One composer require and a config block naming a model. Nothing to sign up to first, and nothing to deploy beside the application it reports on.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
