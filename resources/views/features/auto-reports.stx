<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('features', 'auto-reports')!
const canonical = 'https://reportshq.org/features/auto-reports'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Features', href: '/features' },
  { label: page.title },
]

const sections = [
  {
    "heading": "Describe a model once",
    "body": "Name the model, say which fields are measures and which are dates, and that is the description. An Order with a total and a placed-at is enough for revenue over time, orders per day, and average order value, none of which you asked for individually."
  },
  {
    "heading": "The reports build themselves from that",
    "body": "A described model implies its own reports. Commerce, Customers and Users appear with real numbers already in them, because everything they need was in the description. There is no template gallery to browse and nothing to pick before you see anything."
  },
  {
    "heading": "They obey the application's own rules",
    "body": "A report reads the columns you described, at the grain you described, with the filters you declared. Nothing else is reachable. Ask it for something the description cannot answer correctly \u2014 a measure at a grain it does not have \u2014 and it refuses and says why, rather than returning a number that is quietly wrong."
  },
  {
    "heading": "Then you change them",
    "body": "An auto-created report is an ordinary report. Drag its blocks, change a measure, add one it did not think of. Nothing is regenerated behind you and nothing is locked because it started life automatically."
  }
]
const shot = '/img/feature-auto-reports.png'
const shotAlt = 'A Commerce report showing revenue, order count, revenue by status as a donut and a table, and revenue per day as a line chart'
const shotCaption = 'A Commerce report, drawn by the components that ship. The numbers come from a sample dataset; the charts, the totals and the layout are the real thing.'
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      @include('FeatureShot')

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">Describe one model and watch</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          Name an Order or a User in the config file, run the migration, and open the reports. They are already populated, because the data was already there.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
