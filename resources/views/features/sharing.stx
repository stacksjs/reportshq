<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('features', 'sharing')!
const canonical = 'https://reportshq.org/features/sharing'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Features', href: '/features' },
  { label: page.title },
]

const sections = [
  {
    "heading": "A link, not an account",
    "body": "A share link shows one published report and nothing around it. No navigation into the rest of your project, no sign-in wall, no account for the recipient to forget the password to. The people who most need to see a number are usually the least willing to create a login for it."
  },
  {
    "heading": "Revoking actually revokes",
    "body": "Revoke a link and it stops working on the next request, not at the end of a cache window. Rotate one instead if you want the same report at a new address, which is the honest fix when a URL has been forwarded further than you meant."
  },
  {
    "heading": "Embedding in your own pages",
    "body": "The same report can be embedded in a page of your own, so a client portal or an internal wiki can carry the live chart rather than a screenshot somebody took in March."
  },
  {
    "heading": "You can see whether it was read",
    "body": "Views are recorded against the link, so you know whether the weekly report anybody asked for is being opened. It is a small thing that settles a lot of arguments about what to keep producing."
  }
]
const shot = '/img/feature-sharing.png'
const shotAlt = 'A shared report showing revenue, revenue per day as a line chart and revenue by channel as a donut, with no navigation'
const shotCaption = 'A published report as somebody with no account sees it. Sample numbers; the page, the charts and the link behaviour are the real thing.'
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      @include('FeatureShot')

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">Share a report this afternoon</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          Every plan includes share links. The free plan carries one, which is enough to find out whether the person you are sending it to actually opens it.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
