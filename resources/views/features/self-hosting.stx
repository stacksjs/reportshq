<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('features', 'self-hosting')!
const canonical = 'https://reportshq.org/features/self-hosting'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Features', href: '/features' },
  { label: page.title },
]

const sections = [
  {
    "heading": "The whole application is public",
    "body": "ReportsHQ is a Stacks application and the source is public, including the parts that enforce billing. What you run yourself is the same code, not a hobbled community edition with the useful half removed."
  },
  {
    "heading": "Limits fall away because nothing enforces them",
    "body": "Leave the billing keys unset and every tier limit stops applying. This is not generosity. There is no billing provider configured, so there is nothing to enforce a limit against, and pretending otherwise would just be a nag screen."
  },
  {
    "heading": "What you take on",
    "body": "Your own database, your own backups, your own mail server for scheduled delivery, and your own upgrades. That is the real cost of self-hosting anything, and it is worth stating plainly rather than letting somebody discover it the first time a report fails to send."
  },
  {
    "heading": "The same deployment path we use",
    "body": "The hosted service runs the deploy path that ships in the repository, so the instructions are exercised continuously rather than written once and left to rot."
  }
]
</script>

<template>
  <div class="min-h-[100dvh] text-ink bg-canvas">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="mx-auto px-6 py-10 max-w-3xl">
      <h1 class="font-semibold text-4xl tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 leading-relaxed text-lg text-muted">{{ page.description }}</p>

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="font-semibold text-ink text-xl tracking-tight">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="mt-16 p-8 bg-panel border border-line rounded-2xl">
        <h2 class="font-semibold text-xl tracking-tight">Or let us run it</h2>
        <p class="mt-2 leading-relaxed text-muted text-sm">
          The hosted plans exist so that backups, upgrades and the mail server are somebody else’s evening. The pricing page has the numbers.
        </p>
        <a href="/register" class="inline-block mt-5 px-4 py-2.5 font-medium no-underline text-accent-ink text-sm bg-accent rounded-lg">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
