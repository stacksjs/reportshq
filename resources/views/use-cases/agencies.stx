<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('use-cases', 'agencies')!
const canonical = 'https://reportshq.org/use-cases/agencies'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Use cases', href: '/use-cases' },
  { label: page.title },
]

const sections = [
  {
    "heading": "A project per client, kept apart",
    "body": "Each client is a project with its own events, its own key and its own members. Nothing crosses between them, which is the property you need before you can let a client see anything at all."
  },
  {
    "heading": "A link per stakeholder",
    "body": "Share links show one published report and nothing else in the project. A client sees their report, not your navigation, and revoking a link when an engagement ends actually stops it working."
  },
  {
    "heading": "The monthly report arrives on its own",
    "body": "Schedule the delivery for the morning of the meeting, with the spreadsheet attached. The alternative is assembling it the night before, which is the single most common billable hour that nobody wants to bill for."
  },
  {
    "heading": "Your own footer on the paid tiers",
    "body": "The top tier removes our footer from shared reports, so what you send a client looks like your work. It is stated on the pricing page rather than discovered after the first client sees it."
  }
]
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">One project per client</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          The paid tiers carry enough projects and share links for a small book of clients. The pricing page has the counts.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
