<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('use-cases', 'agencies')!
const canonical = 'https://reportshq.org/use-cases/agencies'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Use cases', href: '/use-cases' },
  { label: page.title },
]

const sections = [
  {
    "heading": "One package per client application",
    "body": "Each client keeps their own data in their own application, and the reports run there. Nothing is pooled, nothing is copied to a central place you would then be responsible for, and no client's numbers can appear in another's report because they were never in the same database."
  },
  {
    "heading": "Reporting you deliver rather than resell",
    "body": "The reports ship inside the application you built, on your client's domain, behind their login. There is no third party for them to sign up to and no separate invoice arriving from a tool they did not choose."
  },
  {
    "heading": "A link for the people who will never log in",
    "body": "A published report shared with a stakeholder who has no account, revocable when the engagement ends, with a note saying which link is which."
  },
  {
    "heading": "Monday morning without you",
    "body": "A schedule mails the report from the client's own application, in the client's own timezone. The status update writes itself, which is the part of the retainer nobody enjoys."
  },
  {
    "heading": "Priced per application",
    "body": "Ten applications on Pro, and the licence is the whole cost. Nothing meters your clients' traffic, because none of it reaches us."
  }
]
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">One project per client</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          The paid tiers carry enough projects and share links for a small book of clients. The pricing page has the counts.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
