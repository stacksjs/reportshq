<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('compare', 'looker-studio')!
const canonical = 'https://reportshq.org/compare/looker-studio'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Comparisons', href: '/compare' },
  { label: page.title },
]

const sections = [
  {
    "heading": "Looker Studio is free and connects to everything",
    "body": "That is a strong combination, and for reporting across several sources at once, marketing spend beside sales beside a spreadsheet somebody maintains by hand, it is hard to argue against."
  },
  {
    "heading": "It needs your data to be reachable",
    "body": "A connector wants credentials into your database, or an export somewhere it can read. Either way the numbers leave the application, and there is now a copy of them living somewhere with its own access rules."
  },
  {
    "heading": "This never moves the data",
    "body": "The report runs inside the application, against its own database, in the same process. There is nothing to connect and nothing to export, so there is no second copy to secure and no sync to be stale."
  },
  {
    "heading": "Reports as code",
    "body": "A report here lives in your repository. What is queryable is declared in your repository, reviewed in a pull request and deployed with the feature it describes. A dashboard built in a browser is a thing somebody made once and nobody can review."
  },
  {
    "heading": "Where Looker Studio wins",
    "body": "Blending sources this cannot see, and giving an analyst somewhere to explore. If the question is about your application's own data, and it is the same question every week, this is the smaller and more honest tool."
  }
]
const compareWith = "Looker Studio"

/*
 * The comparison, row by row.
 *
 * Written so a reader can find the row they came for and stop. Where the other
 * tool is better, the row says so: a comparison page that never concedes a
 * point is an advert, and readers can tell.
 */
const compare = [
  {
    "about": "Where your data goes",
    "ours": "Nowhere. Queried in place, in your own process.",
    "theirs": "Reachable by a connector, or exported somewhere it can read."
  },
  {
    "about": "Blending several sources",
    "ours": "One application at a time. It does not try to be this.",
    "theirs": "Many sources at once. This is what it is for."
  },
  {
    "about": "Freshness",
    "ours": "Live, because it queries the table when you open the page.",
    "theirs": "As fresh as the last connector run."
  },
  {
    "about": "Reports as code",
    "ours": "Reviewed in a pull request, deployed with the app.",
    "theirs": "Built in a browser, versioned by nobody."
  },
  {
    "about": "Access control",
    "ours": "Your application's own login.",
    "theirs": "Google accounts and sharing settings."
  },
  {
    "about": "Working offline or air-gapped",
    "ours": "Yes. No outbound call, including the licence check.",
    "theirs": "No. It is a hosted product."
  },
  {
    "about": "Cost",
    "ours": "Per installed application.",
    "theirs": "Free."
  }
]
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      @include('CompareTable')

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">Fewer moving parts</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          One place that collects the events and draws the charts, rather than a drawing tool plus the pipeline that feeds it.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
