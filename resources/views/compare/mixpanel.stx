<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('compare', 'mixpanel')!
const canonical = 'https://reportshq.org/compare/mixpanel'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Comparisons', href: '/compare' },
  { label: page.title },
]

const sections = [
  {
    "heading": "Mixpanel is the deeper product",
    "body": "Cohort analysis, behavioural segmentation, retention curves, experiment analysis. If your job title contains the words product analyst, Mixpanel does things this does not attempt, and this page is not going to pretend otherwise."
  },
  {
    "heading": "This is smaller on purpose",
    "body": "The bet is that most teams asking for analytics want six correct reports and a link they can send, not a query builder they will use twice. Everything here is arranged around getting to those six sooner, which means less to learn and less to configure."
  },
  {
    "heading": "Where Mixpanel wins",
    "body": "Any question about behaviour over time at the level of individual people. Retention, cohorts, paths through the product, and the volume of documentation and community that comes with a mature tool."
  },
  {
    "heading": "Where this wins",
    "body": "Reports that exist before you configure anything. Prices that do not step by an order of magnitude when your event volume does. Running the whole thing on your own machines, which is not on offer at all."
  }
]
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">Try the smaller thing first</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          If you outgrow it, the events you have been sending are yours and the export is a file, not a support ticket.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
