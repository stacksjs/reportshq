<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('compare', 'build-it-yourself')!
const canonical = 'https://reportshq.org/compare/build-it-yourself'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Comparisons', href: '/compare' },
  { label: page.title },
]

const sections = [
  {
    "heading": "The first version really is a weekend",
    "body": "An events table, an insert, a nightly aggregate and a chart library. Anyone reading this could ship it by Sunday, and for a single number on an internal page that is the correct decision. This page is not going to argue with that."
  },
  {
    "heading": "The second year is the part to price",
    "body": "Retention and pruning so the table does not become the largest thing in your database. Rollups, so a report over last year does not scan every row. Timezones, so the daily job does not double-count an hour twice a year. Then a share link somebody outside the company can open, which means auth, revocation, and a decision about caching."
  },
  {
    "heading": "Where building it wins",
    "body": "Anything specific to your business. If the report you need is a join across three of your own tables with rules only you know, no general tool will express it, and you should write the query."
  },
  {
    "heading": "Where this wins",
    "body": "Everything in the second paragraph is already written, tested and running. And if you decide later that you would rather own it, the source is public and you can."
  }
]
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">The honest version</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          If your reporting needs are one number on one page, build it. That is genuinely a weekend and this would be overkill.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
