<script server>
import { useSeoMeta } from 'stx'
import { metaTitleFor, pageFor } from '../../../app/Marketing/pages'

const page = pageFor('compare', 'plausible')!
const canonical = 'https://reportshq.org/compare/plausible'

useSeoMeta({
  title: metaTitleFor(page.title),
  description: page.description,
  canonical,
  ogUrl: canonical,
  ogTitle: page.title,
  ogDescription: page.description,
  ogType: 'article',
  ogSiteName: 'ReportsHQ',
  twitterCard: 'summary_large_image',
})

const breadcrumbWidth = 'max-w-3xl'
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Comparisons', href: '/compare' },
  { label: page.title },
]

const sections = [
  {
    "heading": "Plausible is web analytics, done carefully",
    "body": "A small script, no cookies, no personal data, a clean dashboard for page views and referrers. For the question of which pages people read and where they came from, it is excellent, and this product does not do it."
  },
  {
    "heading": "This starts after the page view",
    "body": "Events come from your server, not from a script on a page, so the things being counted are orders, signups and refunds rather than visits. An ad blocker cannot remove them and a customer who never loads a marketing page still appears."
  },
  {
    "heading": "Where Plausible wins",
    "body": "Traffic, referrers, and the whole marketing-site side of the question. It is also cheaper, simpler and has nothing to install beyond a script tag."
  },
  {
    "heading": "Where this wins",
    "body": "Money. Anything tied to an order, a plan or a customer record lives in your application, and a page-view tracker cannot see it however carefully it is written."
  }
]
</script>

<template>
  <div class="min-h-[100dvh] bg-canvas text-ink">
    @include('SiteNav')
    @include('Breadcrumbs')

    <main class="px-6 py-10 mx-auto max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tighter md:text-5xl">{{ page.title }}</h1>
      <p class="mt-5 text-lg leading-relaxed text-muted">{{ page.description }}</p>

      <div class="grid gap-10 mt-14">
        @foreach (sections as section)
          <section>
            <h2 class="text-xl font-semibold tracking-tight text-ink">{{ section.heading }}</h2>
            <p class="mt-3 leading-relaxed text-muted">{{ section.body }}</p>
          </section>
        @endforeach
      </div>

      <div class="p-8 mt-16 rounded-2xl border border-line bg-panel">
        <h2 class="text-xl font-semibold tracking-tight">They sit next to each other</h2>
        <p class="mt-2 text-sm leading-relaxed text-muted">
          Plausible for the pages, this for what happened after. Both are small tools that decline to follow people around.
        </p>
        <a href="/register" class="inline-block px-4 py-2.5 mt-5 text-sm font-medium no-underline rounded-lg bg-accent text-accent-ink">
          Start free
        </a>
      </div>
    </main>

    @include('SiteFooter')
  </div>
</template>
